# A single-line comment
#|

This would be a multi-line comment.

|#

# We declare exactly one shared location, named x
shared x;

# We declare a thread-global constant
const global SIZE;

# All threads are writing (and possibly also reading) to position
# the same position of the array, position SIZE.

rw x[SIZE];
